# Observer Pattern

Observer pattern defines a one-to-many dependency between objects.
When the subject state changes, all subscribed observers are notified.
This is often implemented through a publish-subscribe style mechanism.
