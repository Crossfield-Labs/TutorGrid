# Strategy Pattern

Strategy pattern encapsulates algorithms behind a common interface.
It allows selecting or switching algorithm implementations at runtime.
This reduces conditional branching and keeps business logic open for extension.
